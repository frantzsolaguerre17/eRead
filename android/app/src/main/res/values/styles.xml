<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ✅ Thème affiché pendant le démarrage -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- ✅ Thème utilisé après le chargement de Flutter -->
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/deep_purple_background</item>
    </style>

    <!-- ✅ Couleur principale Deep Purple -->
    <color name="deep_purple_background">#512DA8</color>

</resources>
